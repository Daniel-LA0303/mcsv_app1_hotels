
server:
  port: 8086

spring:
  application:
    name: AUTH-SERVICE
  config:
    import: optional:configserver:http://localhost:8885


jwt:
  secret: secret

admin-paths:
  paths:
    -
      uri: "/users"
      method: "POST"